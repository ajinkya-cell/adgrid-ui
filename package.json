{
  "name": "adgrid-ui-monorepo",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "build:registry": "turbo build:registry",
    "release": "turbo build && pnpm --filter void-ui publish --access public"
  },
  "packageManager": "pnpm@10.10.0",
  "devDependencies": {
    "@types/node": "^25.9.3",
    "turbo": "latest",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18",
    "pnpm": ">=8"
  }
}